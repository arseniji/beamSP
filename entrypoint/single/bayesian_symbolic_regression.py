from entrypoint._common import run_method

if __name__ == "__main__":
    run_method(
        title="Вывод формулы: байесовская символьная регрессия (ARD)",
        model_names=["bayes_symreg"],
    )
