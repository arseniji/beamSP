from entrypoint._common import run_method

if __name__ == "__main__":
    run_method(
        title="Экспериментальный блок: Reinforcement Learning (actor-critic)",
        model_names=["rl_ac"],
    )
